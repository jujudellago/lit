<% 
#available_locales = I18n.backend.available_locales 
available_locales = current_user.osdb_languages.collect(&:language_code)

%>


<h3><%= I18n.t('lit.header', default: 'Localization keys') %></h3>

<%= render 'localizations_list', available_locales: available_locales %>


<% if defined?(Kaminari)  %>
  <%= paginate @localization_keys, :theme=>"lit" %>
<%else%>
kaminari not defined !
<% end %>

<%= render 'sidebar' %>

